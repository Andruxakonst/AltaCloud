@page
@model RegisterModel
@{
    ViewData["Title"] = "Регистрация";
}



<div class="row">
    <div class="col-md-12 text-center">
        <div>
            <h1>@ViewData["Title"]</h1>
            <hr />
            <img src="/img/logo_dash.png" alt="Logo_maxi" />
        </div>
        <!--class="col-md-4" -->
        <section>
            <form id="form_center" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <h4>Создать новую учетную запись.</h4>
                <hr />

                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label class="float-left" type="text">Email</label> <!--asp-for="Input.Email" -->
                    <input asp-for="Input.Email" class="form-control" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>

                </div>

                <div class="form-group">
                    <label class="float-left" type="text">Пароль</label> <!-- asp-for="Input.Password" -->
                    <input asp-for="Input.Password" class="form-control" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="float-left" type="text">Подтверждение пароля</label>
                    <input asp-for="Input.ConfirmPassword" class="form-control" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                </div>
                <p style="color:red">Требования к паролю: не менее 6 заков включая  минимум 1 строчную и заглавную букв (A-Z, a-z), цифру (0-9) и не алфавитный символ (!"№;%:?*)</p>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Зарегистрировать</button>
                </div>

            </form>
        </section>
</div>
    <!-- <div class="col-md-6 col-md-offset-2  text-center">
        <section>
            <h4>Страница регистрации в системе <b>Alta-Cloud</b>.</h4>
            <hr />
            <img src="/img/logo_dash.png" alt="Logo_maxi" />

            @{
                if ((Model.ExternalLogins?.Count ?? 0) == 0)
                {
                    <div>
                        <p>
                            Текст доведения информации при регистрации.
                        </p>
                    </div>
                }
                else
                {
                    <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                        <div>
                            <p>
                                @foreach (var provider in Model.ExternalLogins)
                                {
                                    <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                                }
                            </p>
                        </div>
                    </form>
                }
            }
        </section>
    </div>
        -->
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    
}
